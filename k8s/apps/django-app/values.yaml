replicaCount: 2

awsRegion: "ap-south-1"

image:
  repository: "ghcr.io/h1zardian/devsecops-pipeline-project"
  pullPolicy: Always
  tag: "sha-929b48d"
  digest: "sha256:fe47854eb49cb79e8c3e2156c73ffc105814e0b80975e69da624a96a3979ae75"

sqlHost: "devsecops-postgres-dev.cmksk3juhzgm.ap-south-1.rds.amazonaws.com"
allowedHosts: "*"

externalSecrets:
  enabled: true
  environment: "dev"
  secretKey: ""
  postgresPassword: ""

serviceAccount:
  create: true
  name: django-sa
  annotations: {}

# Default: an internet-facing AWS load balancer with an AWS-generated hostname.
# For a custom HTTPS domain, change this to ClusterIP and enable ingress below.
service:
  type: LoadBalancer
  annotations: {}

ingress:
  enabled: false
  className: traefik
  host: app.example.com

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
