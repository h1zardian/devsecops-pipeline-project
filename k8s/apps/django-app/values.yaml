replicaCount: 2

awsRegion: "ap-south-1"

image:
  repository: "ghcr.io/h1zardian/devsecops-pipeline-project"
  pullPolicy: Always
  tag: "sha-af6d674"
  digest: "sha256:1c4d76cfdb7b1869d6b91a449f2734f9b6b6d32841d8d913cd6e3cdd42d77260"

sqlHost: "devsecops-postgres-dev.cmksk3juhzgm.ap-south-1.rds.amazonaws.com"
allowedHosts: "*"

externalSecrets:
  enabled: true
  environment: "dev"
  secretKey: ""
  postgresPassword: ""

serviceAccount:
  create: true
  name: django-sa
  annotations: {}

# Default: an internet-facing AWS load balancer with an AWS-generated hostname.
# For a custom HTTPS domain, change this to ClusterIP and enable ingress below.
service:
  type: LoadBalancer
  annotations: {}

ingress:
  enabled: false
  className: traefik
  host: app.example.com

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
