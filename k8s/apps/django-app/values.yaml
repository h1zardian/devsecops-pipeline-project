replicaCount: 2

awsRegion: "ap-south-1"

image:
  repository: "ghcr.io/h1zardian/devsecops-pipeline-project"
  pullPolicy: Always
  tag: "sha-f338c6e"
  digest: "sha256:3b17670f76da6f25b0bb965bf959472fe65b9889cd525c99b7ff5e829b0edfa3"

sqlHost: "devsecops-postgres-dev.cmksk3juhzgm.ap-south-1.rds.amazonaws.com"
allowedHosts: "*"

externalSecrets:
  enabled: true
  environment: "dev"
  secretKey: ""
  postgresPassword: ""

serviceAccount:
  create: true
  name: django-sa
  annotations: {}

# Default: an internet-facing AWS load balancer with an AWS-generated hostname.
# For a custom HTTPS domain, change this to ClusterIP and enable ingress below.
service:
  type: LoadBalancer
  annotations: {}

ingress:
  enabled: false
  className: traefik
  host: app.example.com

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
